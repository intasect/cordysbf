
# Product options
product.vendor		= Cordys CoE

# The project name and version will be combined to name the jar file.
#project.name		= AntTaskTestProject
project.name		= InterpayBrievenModule
project.version 	= 1.0
project.web.name	= coe/ant-task-test
project.jar.name	= ant-task-test
project.deploy		= \\coe\\ant-task-test

# Build options
clean.build		= on

# Compiler options.

debug         	= on
optimize      	= on
deprecation   	= on
jvm.target.version = 1.4

# Javadoc options.

javadoc.packagename	= com.cordys.coe
javadoc.author  	= true
javadoc.private 	= true
javadoc.use     	= true
javadoc.version 	= true
javadoc.windowtitle	= ${project.name} API
javadoc.doctitle	= ${project.name} ${project.version}

# ISV package properties
isv.owner		= Cordys CoE
isv.productname	= ${project.name}
isv.version		= ${project.version}
isv.name		= ${isv.owner} ${project.name} ${isv.version}
isv.filename	= ${isv.owner}_${project.name}_${isv.version}
isv.content		= roles,menus,methodsets,xmlstore

# Name of the LDAP server to connect to
ldap.server = server
ldap.port = 3899
ldap.user = cn=Directory Manager,o=vanenburg.com
ldap.password = ldappw

# ECX server
cordys.home = c:/Cordys

# The following organization definition is for the build server
# if you need a different organization, create a file called
# user.properties in the root of the project, and set the
# isv.org property to your custom value. This file will not be
# checked-in, into the CVS.

isv.org			= o=ant-test,cn=cordys,o=vanenburg.com
isv.userdn		= cn=${env.USERNAME},cn=organizational users,${isv.org}
content.org			= o=ant-test,cn=cordys,o=vanenburg.com
content.userdn		= cn=${env.USERNAME},cn=organizational users,${content.org}
content.filter		= methodsets,roles,menus,applicationconnectors,xmlstore,styles,xforms,toolbars

# Where source files are found

src.dir  	= ./src
src.java 	= ${src.dir}/java
src.web 	= ${src.dir}/web
src.dbschema= ${src.dir}/dbschema
src.content	= ${src.dir}/content
src.content.appcon = ${src.content}/applicationconnectors
src.content.coboc = ${src.content}/coboc
src.content.dbschema = ${src.content}/dbschema
src.content.isv = ${src.content}/isv
src.content.menus = ${src.content}/menus
src.content.methodsets = ${src.content}/methodsets
src.content.roles = ${src.content}/roles
src.content.studio = ${src.content}/studio
src.content.styles = ${src.content}/styles
src.content.xforms = ${src.content.studio}/xforms
src.content.xmlstore = ${src.content}/xmlstore

# CoBOC folder content path filter. Used to limit the
# operations to only a specific subtree. 
coboc.root.path = /

# Studio BPM content path filter. Used to limit the
# operations to only a specific subtree. 
studio.bpm.root.path = /3.Business Process Models

# Where test files are found

test.dir  	= ./test
test.java 	= ${test.dir}/java

# Where documentation is found

docs.dir	= ./docs
docs.internal	= ${docs.dir}/internal
docs.external	= ${docs.dir}/external

# Where jar files are found.

lib.dir	= ./lib
sdk.dir	= ./sdk

# Intermediate directory for building class files.

build.dir     	= ./build
build.classes 	= ${build.dir}/classes
build.jar	  	= ${build.dir}/jar
build.web	  	= ${build.dir}/web
build.test    	= ${build.dir}/test
build.docs    	= ${build.dir}/documentation
build.apidocs 	= ${build.dir}/apidocs
build.isv		= ${build.dir}/isv
build.content	= ${build.dir}/content
build.junitreports 	= ${build.dir}/junit-reports
build.test.classes = ${build.dir}/testclasses
build.bin		= ${build.dir}/bin

# The resulting Studio vcmdata file that has 
# all the vcmdata files merged into it.
build.studio.isv.filename = ${build.dir}/${isv.owner}_${project.name}_${isv.version}.vcmdata

# Where the distributable of the project is placed.

dist.dir      	= ./dist
dist.docs     	= ${dist.dir}/docs
dist.apidocs	= ${dist.docs}/api
dist.setup    	= ${dist.dir}/setup

# VSS Integration
vss.ss.path = \\\\srv-nl-apps7\\e$\\VSS\\win32
vss.inifile.path = \\\\srv-nl-apps7\\e$\\VSS
vss.project = /coe/projectnamt
vss.username = ${env.USERNAME}
vss.password = ${env.USERNAME}
vss.labelname = "Build"

# SubVersion Integration
svn.project = http://srv-nl-apps7:5001/svn/coe_rep/ant-task-test
svn.main = ${svn.project}/main
svn.branches = ${svn.project}/branches
svn.label = build

#TO BE SET IN THE user.properties.
#svn.username = svnuser
#svn.password = svnpassword

#Uncomment these 2 in the user.properties if you want to label the build.
#using.svn = true
#using.vss = true
#label.build=defined